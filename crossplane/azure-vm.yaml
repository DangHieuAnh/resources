apiVersion: compute.platform.io/v1alpha1
kind: AzureVM
metadata:
  name: my-dev-server
  namespace: ns-tenant-domain    # ← auto-injected
spec:
  vmSize: "Standard_B2s"
  region: "eastus"
  diskSizeGb: 64
  providerConfigRef:
    name: config-tenant-azure    # ← auto-injected